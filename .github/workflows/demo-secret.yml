name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        env:
          SECRET1: ${{ secrets.SECRET1 }}
        run: if [[ $SECRET1 == "Secret" ]]; then echo "Success"; else echo "Failure"; fi
