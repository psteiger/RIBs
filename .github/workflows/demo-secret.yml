name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        env:
          SECRET1: ${{ secrets.SECRET1 }}
        run: echo "The secret is $SECRET1"
